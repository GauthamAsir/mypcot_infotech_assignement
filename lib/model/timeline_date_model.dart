class TimelineDateModel {
  int month, date, year;
  String weekName;

  TimelineDateModel(
      {required this.month,
      required this.date,
      required this.weekName,
      required this.year});
}
